{
  "name": "Sequential-Numbers",
  "version": "1.0.0",
  "description": "A Figma plugin that sequentially increments numbers in selected frames",
  "main": "./dist/code.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "npm run build -- --watch"
  },
  "author": "Xavier Valarino",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "typescript": "*"
  }
}
